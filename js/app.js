$( document ).ready(function() {
    
  const cardBox = document.getElementsByClassName('card')[0];

  function createBox(){

    cardBox.appendChild(1);
  }

  cardBox.appendChild(1);
  cardBox.appendChild(1);
  cardBox.appendChild(1);
  


});